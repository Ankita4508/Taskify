<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0"> <!-- Responsive scaling -->
  <style>
    body {
      margin: 0;
      padding: 0;
      width: 100%;
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: center;
      background: linear-gradient(135deg, #667eea, #764ba2);
      font-family: "Segoe UI", sans-serif;
    }

    /* Login container */
    .login-container {
      display: flex;
      justify-content: center;
      align-items: center;
      width: 100%;
      padding: 20px;
    }

    .login-box {
      background: white;
      padding: 2rem;
      border-radius: 1rem;
      box-shadow: 0 8px 24px rgba(0,0,0,0.2);
      width: 100%;
      max-width: 350px; /* never grows wider */
      text-align: center;
    }

    .login-box h1 {
      margin-bottom: 1rem;
      color: #333;
    }

    .login-box input {
      width: 100%;
      padding: 0.75rem;
      margin-bottom: 1rem;
      border: 1px solid #ddd;
      border-radius: 0.5rem;
      font-size: 1rem;
      box-sizing: border-box; /* ✅ prevents overflowing */
    }

    .login-box button {
      width: 100%;
      padding: 0.75rem;
      border: none;
      border-radius: 0.5rem;
      background: #667eea;
      color: white;
      font-weight: bold;
      font-size: 1rem;
      cursor: pointer;
      transition: background 0.3s ease;
    }

    .login-box button:hover {
      background: #5a67d8;
    }

    /* Branding */
    .brand {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin-bottom: 2rem;
    }

    .brand img {
      width: 64px;
      height: 64px;
      margin-bottom: 0.8rem;
    }

    .brand h1 {
      margin: 0;
      font-size: 1.6rem;
      font-weight: 700;
      color: #111827;
    }

    .brand .tagline {
      font-size: 0.9rem;
      color: #6b7280;
      margin-top: 0.2rem;
    }

    .login-box p {
      margin-top: 1rem;
      font-size: 0.9rem;
    }

    .login-box a {
      color: #667eea;
      text-decoration: none;
      font-weight: bold;
    }

    .login-box a:hover {
      text-decoration: underline;
    }

    /* ✅ Responsive adjustments */
    @media (max-width: 480px) {
      .login-box {
        padding: 1.5rem;
        max-width: 90%; /* uses almost full width on mobile */
      }

      .brand img {
        width: 50px;
        height: 50px;
      }

      .brand h1 {
        font-size: 1.4rem;
      }

      .login-box input, 
      .login-box button {
        font-size: 0.95rem;
        padding: 0.65rem;
      }
    }
  </style>
</head>
<body>
   <div class="login-container" id="login">
    <div class="login-box">
      <!-- Branding -->
      <div class="brand">
        <img src="https://cdn-icons-png.flaticon.com/512/3209/3209265.png" alt="Taskify Logo" />
        <h1>Taskify</h1>
        <p class="tagline">Smart Task Manager</p>
      </div>

      <form method="POST" action="/forgot-password">
        <p style="font-weight: bold; text-align: left;">Forgot Password??</p>

        <% if (error) { %>
          <div class="error"><%= error %></div>
        <% } %>

        <% if (success) { %>
          <div class="message"><%= success %></div>
        <% } %>

        <input type="email" name="email" placeholder="Enter your email" required />
        <button type="submit">Send OTP</button>

        <p><a href="/login">Back to Login</a></p>
      </form>
    </div>
   </div>
</body>
</html>
